[**ts-openapi-express**](../../../README.md)

---

[ts-openapi-express](../../../README.md) / [utils/algos](../README.md) / sha256

# Function: sha256()

> **sha256**(`input`): `Promise`\<[`ByteArray`](../../../types/type-aliases/ByteArray.md)\>

Computes the SHA-256 hash of the input data.

## Parameters

### input

[`ByteArray`](../../../types/type-aliases/ByteArray.md)

The data to hash.

## Returns

`Promise`\<[`ByteArray`](../../../types/type-aliases/ByteArray.md)\>

A promise that resolves to the SHA-256 hash as a byte array.

## Example

```typescript
const hash = await sha256(new Uint8Array([1, 2, 3]))
```
